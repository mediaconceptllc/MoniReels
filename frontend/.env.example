# The Railway API's public URL. Baked in at build time, so a change needs a
# redeploy — which is correct: it is part of what this build talks to, not a
# runtime setting a user should be able to point elsewhere.
NEXT_PUBLIC_API_URL=https://monireels-api.up.railway.app
